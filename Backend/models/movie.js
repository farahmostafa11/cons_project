/*
const mongoose = require('mongoose');

const movieSchema = new mongoose.Schema({
    title: {
        type: String,
        required: true, // title is the key
        unique: true
    },
    date: {
        type: Date,
        required: true
    },
    poster: {
        type: String,
        required: true
    },
    screeningRoom: {
        type: String,
        required: true
    },
    startTime: {
        type: Time,
        require: true,
    },
    endTime: {
        type: Date,
        require: true,
    }
});
  
module.exports = mongoose.model('Movie', movieSchema);
*/